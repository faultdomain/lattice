[package]
name = "lattice-workload"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Shared workload compilation pipeline for Lattice CRDs"

[features]
fips = ["lattice-common/fips", "aws-lc-rs/fips"]

[dependencies]
# Internal
lattice-common = { workspace = true }
lattice-core = { workspace = true }
lattice-crd = { workspace = true }
lattice-graph = { workspace = true }
lattice-cedar = { workspace = true }
lattice-secret-provider = { workspace = true }
lattice-quota = { workspace = true }
lattice-render = { workspace = true }
lattice-template = { workspace = true }

# Crypto (FIPS)
aws-lc-rs = { workspace = true }
sigstore = { workspace = true }

# Serialization
serde = { workspace = true }
serde_json = { workspace = true }

# Error handling
thiserror = { workspace = true }

# Async
tokio = { workspace = true }

# Logging
tracing = { workspace = true }

[dev-dependencies]
tokio-test = { workspace = true }
tempfile = { workspace = true }
